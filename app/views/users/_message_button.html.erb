<% if user.id != current_user.id %> 
  <% if chat %>
    <%= button_to chat_path(chat),
                  method: :get,
                  aria: "Send a message" do %>
      <i class="bi bi-chat-square-text" title="Send a message."></i>
    <% end%>
  <% else %>
    <%= button_to chats_path(recipient_id: user.id),
                  method: :post,
                  aria: "Send a message" do %>
      <i class="bi bi-chat-square-text" title="Send a message."></i>
    <% end%>
  <% end %>
<% end %>