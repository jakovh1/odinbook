<% if user.image.attached? %>
  <%= image_tag(user.image, size: "80x80") %>
<% else %>
  <%= image_tag(gravatar_avatar_url(user.email)) %>
<% end %>

<% if current_user.id == user.id %>
  <%= render "upload_avatar_form", user: user %>
<% end %>