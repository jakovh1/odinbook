<% content_for :title, "Comments" %>

<div id="comments">
  <% comments.each do |comment| %>
    <%= render comment %>
  <% end %>
</div>

<% if false %>
  <%= link_to "New comment", new_comment_path %>
<% end %>